const config = {
  database: {
    drive: 'mysql' /* one of 'mysql' | 'mariadb' | 'postgres' | 'mssql' */,
    host: 'localhost',
    database: 'todo_api',
    user: 'myuser',
    password: 'mypass'
  }
}

module.exports = config
